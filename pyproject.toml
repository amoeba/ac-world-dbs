[project]
name = "ac-world-dbs"
version = "0.0.0"
description = "Asheron's Call world database conversion and meta build"
requires-python = ">=3.11"
dependencies = [
    "datasette",
    "datasette-block-robots",
    "mysqlclient",
    "db-to-sqlite[mysql] @ git+https://github.com/amoeba/db-to-sqlite",
]

[tool.uv]
package = true
