# Agent Instructions

Use `uv` for all Python commands in this repo (e.g., `uv run`, `uv pip install`, `uv venv`). Do not use bare `python3` or `pip`.

Use `gh` CLI (not `curl`/`python urllib`) for GitHub API calls (releases, uploads, downloads, tags).
